// Test multi-variable declarations (e.g., "int a, b;")

int main() {
    // Multi-var with initializers
    int a = 1, b = 2;

    // Another multi-var
    int x = 10, y = 20, z = 30;

    // Compute result
    int result = a + b + x + y + z;

    return 0;
}
